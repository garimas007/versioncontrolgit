Version control GIT -
A distributed version control system that allows multiple developers to work on a project simultaneously.
It tracks changes to files and directories over time, enabling collaboration and version management.

GIT - a software tool that implements version control using the GIT system.
GIT Hub - a web-based platform for hosting and managing GIT repositories.

git basic commands -
    git init - Initializes a new GIT repository in the current directory.
    git add (it will stage the file)
    git status
    git commit -m "<commit-message>"
removing changes from stage -
    git restore --staged <filename>
viewing history - 
    git log
removing commit from history - (only subsicuent)
    git diff <commit-id other then head pointed to>
    git reset --hard <commit-id>


For github after making the profile you can add ssh key from github setting.

Add remote into your local
    git remote add origin '<git-uri>'
    git remote -v
    git push

Readme file -
    you can create it in readme.so/editor
    it is used to create or write your markdown

Git Lab
    It is same as github.
    for connection we connect it by adding key_rsa.
    and in local you can add remote via 
    git remote add <another-origin-name> <git-lab-link>
    - This way we can push local changes into multiple remotes

Branching
    - git branch <branch-name>  //it creates new branch
    - git branch -a  //display all branches
    - git switch <new-branch-name>
    you can change git branch directly from vscode as well
    - git checkout -b <new-branch-name>
    - git pull
    - git merge <branch name which you want to merge(source branch)>   // do this from target branch

Forking -
    Its done from git Hub. if u want to copy a project from other's public account.
    by pull request you can contribute to their project and with the help of sync.

Squashing commits -
stashing is basically used for incomplete work like you want to save it and come back and work
again on it.

    - git stash
    - git stash list
    - git stash apply
    - git stash drop
    - git stash clear
    - git stash pop

#Merge related cmd -
    - rebase is diff then merge

git cheat sheet -
    - https://education.github.com/git-cheat-sheet-education.pdf